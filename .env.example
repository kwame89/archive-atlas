VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Stellar network for wallet linking, signing, and explorer links.
# "testnet" (default) or "mainnet". Must match the anchor-event function's
# STELLAR_NETWORK secret — see README > Mainnet cutover.
VITE_STELLAR_NETWORK=testnet
